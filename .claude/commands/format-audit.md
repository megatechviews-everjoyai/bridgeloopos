# /format-audit [file-path]
**Purpose**: Converts raw business audit notes into a structured MegaViews YouTube script.
**Action**: Executes the `scripts/format_audit.py` script on the provided file path and saves the formatted version to `outputs/megaviews-media/`.